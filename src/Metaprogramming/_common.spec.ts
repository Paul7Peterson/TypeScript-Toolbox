
import { aFunction } from '@paul7peterson/typescript-toolbox';
import { joinLines } from './_common';

describe(aFunction(joinLines), () => {
  it('passes', () => {

    expect(true).toBe(true);
  });
});





