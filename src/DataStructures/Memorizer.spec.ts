import { aClass } from '@paul7peterson/typescript-toolbox';
import { Memorizer } from './Memorizer';

describe(aClass(Memorizer), () => {

  it('passes', () => {
    expect(true).toBe(true);
  });
});
